import styled from 'styled-components'

export const TrackMatrix = styled.div`
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 7px;
    font-size: 13px;
    color: var(--text-grey);
`